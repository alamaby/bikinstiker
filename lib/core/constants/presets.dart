// App-wide sticker constants.
// Preset data is now fetched from Supabase via list-presets Edge Function.
// See lib/data/models/sticker_preset.dart for the StickerPreset model.

const int kStickerCost = 1;
const int kMaxPromptChars = 200;
